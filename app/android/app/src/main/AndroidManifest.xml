<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Milestone 3 reminders. Every one of these is load-bearing; see
         docs/adr/0011-free-tier-only.md for why the schedule lives on the
         device at all rather than on a server.

         POST_NOTIFICATIONS  - Android 13+ requires this at runtime. Without
                               the prompt being accepted, scheduling silently
                               succeeds and nothing is ever shown.
         SCHEDULE_EXACT_ALARM - Android 12+ gate on alarms that fire at a
                               precise time. Not auto-granted; the user has to
                               allow it in system settings. If they do not, the
                               app falls back to inexact scheduling and Doze may
                               delay a reminder by an unpredictable amount.
                               USE_EXACT_ALARM would be auto-granted, but it
                               declares the app to be an alarm-clock-class app,
                               which this is not.
         RECEIVE_BOOT_COMPLETED - pending alarms do not survive a reboot. The
                               plugin's boot receiver re-registers them; without
                               this permission every reminder is lost the first
                               time the phone restarts, which is the kind of
                               failure nobody notices until it matters.
         VIBRATE             - so a reminder is felt as well as seen. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:label="zavithar_manager"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
