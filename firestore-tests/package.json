{
  "name": "zavithar-firestore-tests",
  "private": true,
  "type": "module",
  "description": "Security-rules tests. Run with: pnpm test",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-zavithar \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
