# Devlog — 2026-08-22

**Session goal:** Start Milestone 0 (scaffolding). Plan the work, get the toolchain installed.

## What we did

1. **Planned Milestone 0** and settled the decisions CLAUDE.md left open (see `docs/adr/` — still to be written up):
   - State management: `flutter_riverpod`
   - Routing: `go_router` with `StatefulShellRoute`
   - Repo layout: monorepo — `app/` (Flutter) + `functions/` (Cloud Functions), Firebase config at root
   - M0 scope: **Android first**, Windows verified afterwards
   - This project is a documentation-first *learning* build — docs and lessons are deliverables, not cleanup

2. **Created the repo skeleton**: `app/`, `functions/`, `claude/`, `docs/{adr,devlog,learning}`, plus `README.md` and `.gitignore`.

3. **Moved the project docs into `claude/`** — the PRD, architecture, data model, and all the phase plans, pasted in from the Claude Project.

4. **Installed Flutter 3.47.1** (stable, released 2026-08-19, Dart 3.13.1):
   - Downloaded to `%USERPROFILE%\develop\flutter_windows_3.47.1-stable.zip` (1.8 GB), verified with `unzip -t`, extracted to `%USERPROFILE%\develop\flutter`.
   - Added `%USERPROFILE%\develop\flutter\bin` and `%LOCALAPPDATA%\Pub\Cache\bin` to the **user** PATH.
   - Set `ANDROID_HOME` to `%LOCALAPPDATA%\Android\Sdk` (it was unset).

5. **Ran `flutter doctor -v`** — results below.

## `flutter doctor` state at end of session

| Check | Result |
|---|---|
| Flutter 3.47.1 | ✅ Installed and running |
| Windows 11 25H2 | ✅ |
| Android toolchain | ⚠️ **Blocked** — see below |
| Chrome (web) | ❌ Not found — **irrelevant**, web is not a target (PRD non-goal) |
| Visual Studio | ⚠️ Build Tools 2022 17.14.19 present but **incomplete** |
| Connected devices | ✅ Windows desktop, Edge — **no Android device or emulator yet** |
| Network resources | ✅ |

## Open items for next session

1. **Android SDK 36 required.** Only `platforms/android-34` is installed; Flutter 3.47.1 wants SDK 36 + build-tools. Fix with `sdkmanager "platforms;android-36" "build-tools;36.0.0"`.
2. **Android licenses not accepted.** Run `flutter doctor --android-licenses` — interactive, needs a human pressing `y`.
3. **No Android device.** The emulator binary and system images are installed but no AVD is set up yet. Either create an AVD or plug in the phone with USB debugging on.
4. **Firebase CLI not installed.** `npm install -g firebase-tools` was started but cancelled at end of session.
5. **Firebase project doesn't exist yet.** Needs the console + a browser login — hands-on step.
6. **PATH not verified in a fresh shell.** `flutter doctor` still warned the binary wasn't on PATH, because the running shell predates the change. Confirm in a *new* terminal that `flutter --version` works bare.

## Correction to the plan

The plan recorded "no Visual Studio products installed — the BuildTools folders are empty stubs," based on `vswhere` returning nothing. **`flutter doctor` found an actual install**: Visual Studio Build Tools 2022 17.14.36623.8 with Windows 10 SDK 10.0.26100.0, flagged as *incomplete*.

This makes the Windows step (Step 8) cheaper than planned — it's **adding the "Desktop development with C++" workload to an existing install** via the Visual Studio Installer, not a fresh 7–10 GB download.

## Notes / observations

- The user PATH contains a large duplicated block and a stray `C:\P` entry. Pre-existing, harmless, not touched — but worth a cleanup someday.
- **Requirements conflict spotted while reading the PRD**, relevant at Milestone 3 (not a blocker now, but it needs a decision before M3 starts):
  - **NFR-5** says the app should run on Firebase's free **Spark** plan, but **FR-16** requires Cloud Functions — which have required the **Blaze** plan at any volume since Feb 2026. Blaze stays at $0 within the free tier but requires a card on file.
  - **FR-15** requires push notifications on **both** devices with the app closed, but `firebase_messaging` **has no Windows support**. FCM cannot deliver to the Windows desktop at all.
  - Related: `google_sign_in` has no Windows support either, so Windows sign-in must use email/password — which FR-2 already allows, so no conflict there.
